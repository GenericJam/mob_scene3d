# Mix build artifacts
/_build/
/cover/
/deps/
/doc/
erl_crash.dump
*.ez
mob_scene3d-*.tar
/tmp/

# Vendored host tools (scripts/fetch_cmgen.sh) + downloaded sample corpora
# (scripts/fetch_gltf_samples.sh) — fetched, never committed
/vendor/

# Local config — never committed (AGENTS.md)
mob.exs
.envrc

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
